1) 
npm install express ejs mongoose

* permite generar un COMPONENTE y con --dry-run  se ve y revisa previamente, 
ng generate component module-author/view-quotes --dry-run



error al conectar mongoose por nuevo nodejs :
    mongoose.connect("mongodb://0.0.0.0:27017/authorQuotesDB", { useNewUrlParser: true });